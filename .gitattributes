# Auto detect text files and perform LF normalization
* text=auto
turtle_low_M_Turtle_Normal.uasset filter=lfs diff=lfs merge=lfs -text

# Editor binaries are committed for the double-click .uproject workflow
*.dll binary
*.modules text
